import { Loader } from "excalibur"

/**
 * Resource loader for Excalibur.js
 */
export function createResourceLoader(): Loader {
  const loader = new Loader()

  // Add resources here
  // Example:
  // loader.addResource(spriteSheet)
  // loader.addResource(soundFile)

  return loader
}
